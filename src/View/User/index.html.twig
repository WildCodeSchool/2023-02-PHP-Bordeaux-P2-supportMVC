{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Users</h1>
    <ul>
        {% for user in users %}
            <li><a href="/users/show?id={{ user.id }}">{{ user.p2_firstname }} {{ user.p2_lastname }} né(e)
                    le {{ user.p2_birthday|date("d/m/Y") }}</a>
            </li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
    <a href="/users/add">Add user</a>
{% endblock %}
